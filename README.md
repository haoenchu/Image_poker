下注計畫:
填寫本金(>1000) ->確認 ->無法修改 
-> 押注金額(最低100元) -> 列表填寫要押注哪種牌型 ->是      ->照倍率給 
                                              ->不是    ->沒收下注金額 
                                              (在總金額那邊改變數字)


等到玩到沒錢了 -> 直接跳掉?
